# 🛒 LuxeMarket Frontend

The interactive user interface for LuxeMarket, built with **React 19** and **Vite**.

## 🚀 Getting Started

1. **Install Dependencies**:
   ```bash
   npm install
   ```

2. **Run Development Server**:
   ```bash
   npm run dev
   ```

3. **Build for Production**:
   ```bash
   npm run build
   ```

## 🛠️ Features Implemented
- **Responsive Layout**: Mobile-first design using Tailwind CSS.
- **State Management**: React hooks for cart and authentication state.
- **Routing**: Client-side routing with `react-router-dom`.
- **API Integration**: Robust communication with the Node.js backend using Axios.
- **Modern Icons**: Lucide-react for a consistent, premium look.

## 📁 Project Structure
- `src/api`: Axios instance configuration.
- `src/components`: Reusable UI components (Navbar, Product Cards, etc.).
- `src/pages`: Main application views (Home, Cart, Orders, Login).
- `src/App.jsx`: Main routing and entry point.
